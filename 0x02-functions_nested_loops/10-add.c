#include <stdio.h>
#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 * @a: first parameter
 * @b: second parameter
 * Discription: a function that adds two integers
 * Return: 0
 */
int add(int a, int b)
{
	return (a + b);
}
