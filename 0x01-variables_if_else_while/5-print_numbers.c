#include <stdio.h>
/**
 * main - Entry point
 *
 * Discription: single digit with the base ten
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)

		printf("%d", i);

	printf("/n");
	return (0);
}
