#include <stdio.h>
#include "main.h"
/**
 * postitive_or_negative - checks wheather it is positive or negative
 *
 * Discription: checks wheather it is postive or negative
 * Return: o
 */
void positive_or_negative(int i)
{
	if (i >= 0)
	{
		printf("%d is zero", i);
	}
	printf("\n");
}
