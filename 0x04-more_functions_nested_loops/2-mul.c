#include <stdio.h>
#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: first number
 * @b: second number
 * Return: return nothing
 */
int mul(int a, int b)
{
	int a, b

	int result = a * b;

	return (result);
}
