#include <stdio.h>
#include "main.h"

/**
 * mul(int, int) - function that multiplies two integers
 * @x: first number
 * @y: second number
 * Return: Always 0
 */
int mul(int x, int y)
{
	return (x * y);
}
